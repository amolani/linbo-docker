---
phase: 02-secrets-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - containers/api/src/index.js
  - containers/api/tests/startup-validation.test.js
  - .gitignore
  - config/rsyncd.secrets.example
autonomous: true
requirements: [PROD-02, PROD-05]

must_haves:
  truths:
    - "API exits with code 1 when JWT_SECRET has a default value and NODE_ENV=production"
    - "API exits with code 1 when INTERNAL_API_KEY has a default value and NODE_ENV=production"
    - "API logs warning but continues when defaults are used in NODE_ENV=development"
    - "rsyncd.secrets is no longer tracked by git"
    - "rsyncd.secrets.example exists with placeholder content"
  artifacts:
    - path: "containers/api/src/index.js"
      provides: "validateSecrets function called before server.listen"
      contains: "validateSecrets"
    - path: "containers/api/tests/startup-validation.test.js"
      provides: "Unit tests for validateSecrets behavior"
      min_lines: 40
    - path: "config/rsyncd.secrets.example"
      provides: "Placeholder rsync secrets file"
      contains: "linbo:CHANGE_ME"
  key_links:
    - from: "containers/api/src/index.js"
      to: "process.env.JWT_SECRET"
      via: "validateSecrets checks against known default array"
      pattern: "JWT_SECRET_DEFAULTS"
    - from: "containers/api/src/index.js"
      to: "process.env.INTERNAL_API_KEY"
      via: "validateSecrets checks against known default array"
      pattern: "INTERNAL_KEY_DEFAULTS"
    - from: "containers/api/src/index.js"
      to: "process.exit(1)"
      via: "production mode with defaults triggers fatal exit"
      pattern: "process\\.exit\\(1\\)"
---

<objective>
Add production startup validation for secrets and clean up tracked rsyncd.secrets from git.

Purpose: Prevent the API from running in production with insecure default credentials, and remove a tracked secrets file from the repository.
Output: validateSecrets() function in index.js with tests, rsyncd.secrets untracked with .example file.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-secrets-hardening/02-RESEARCH.md

<interfaces>
<!-- Key integration points the executor needs -->

From containers/api/src/index.js (line 160-161, insertion point):
```javascript
async function startServer() {
  console.log('Starting LINBO Docker API Server...\n');
  // INSERT validateSecrets() HERE, before any connections
  // Connect to database (optional)
  if (prisma) { ... }
}
```

From containers/api/tests/globalSetup.js:
```javascript
// Tests run with NODE_ENV=test, JWT_SECRET='test_jwt_secret_for_testing_only'
// validateSecrets must NOT block in test mode
```

From containers/api/tests/setup.js:
```javascript
// console.log, console.debug, console.info are suppressed in tests
// console.warn and console.error are NOT suppressed
```

Known default values to detect (cataloged from codebase):
- JWT_SECRET defaults: 'linbo-docker-secret-change-in-production', 'your_jwt_secret_here_change_in_production', 'your_jwt_secret_here_change_me_in_production_use_openssl_rand', 'development_secret_change_in_production'
- INTERNAL_API_KEY defaults: 'linbo-internal-secret', 'linbo-internal-secret-change-in-production'

From .gitignore (lines 9-10):
```
*.secrets
!config/rsyncd.secrets
```
The exception on line 10 must be removed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add validateSecrets() to API startup with tests</name>
  <files>containers/api/src/index.js, containers/api/tests/startup-validation.test.js</files>
  <behavior>
    - Test 1: validateSecrets() calls process.exit(1) when NODE_ENV=production and JWT_SECRET is a known default ('linbo-docker-secret-change-in-production')
    - Test 2: validateSecrets() calls process.exit(1) when NODE_ENV=production and INTERNAL_API_KEY is a known default ('linbo-internal-secret')
    - Test 3: validateSecrets() calls process.exit(1) when NODE_ENV=production and JWT_SECRET is undefined/empty
    - Test 4: validateSecrets() calls process.exit(1) when NODE_ENV=production and INTERNAL_API_KEY is undefined/empty
    - Test 5: validateSecrets() does NOT exit when NODE_ENV=production and both secrets have non-default values
    - Test 6: validateSecrets() logs warning (console.warn) but does NOT exit when NODE_ENV=development and defaults are used
    - Test 7: validateSecrets() does NOT exit when NODE_ENV=test and defaults are used
  </behavior>
  <action>
Create `containers/api/tests/startup-validation.test.js` with tests that exercise a `validateSecrets` function exported via `_testing` from index.js. Tests must mock `process.exit` (jest.spyOn) and `console.warn`/`console.error`. Each test must set `process.env.NODE_ENV` and the relevant secret env vars, call `validateSecrets()`, then assert exit/warn behavior. Clean up env vars in afterEach.

Then add `validateSecrets()` to `containers/api/src/index.js`:
- Define at module level (before `startServer`), NOT inside startServer
- Use two arrays of known default strings: `JWT_SECRET_DEFAULTS` and `INTERNAL_KEY_DEFAULTS` (values from the interfaces block above)
- Check `process.env.JWT_SECRET` and `process.env.INTERNAL_API_KEY` against these arrays AND against undefined/empty
- If NODE_ENV=production and defaults found: `console.error` with FATAL message naming offending variables, then `process.exit(1)`
- If NODE_ENV is NOT production and defaults found: `console.warn` per variable
- Call `validateSecrets()` as the first line inside `startServer()`, after the initial console.log

Export for testing: add `validateSecrets` to a `_testing` export at the bottom of index.js:
```javascript
// Test helpers
module.exports._testing = { validateSecrets };
```

IMPORTANT: Do NOT validate ADMIN_PASSWORD or DB_PASSWORD (per user decision). Do NOT add CORS_ORIGIN validation (Phase 3 scope).
  </action>
  <verify>
    <automated>cd /root/linbo-docker/containers/api && npx jest tests/startup-validation.test.js --runInBand -x</automated>
  </verify>
  <done>All 7 test cases pass. validateSecrets exits with code 1 in production with default secrets, warns in development, does nothing in test mode. Function is called at the top of startServer().</done>
</task>

<task type="auto">
  <name>Task 2: Remove rsyncd.secrets from git tracking and create example file</name>
  <files>.gitignore, config/rsyncd.secrets.example</files>
  <action>
1. Remove the `!config/rsyncd.secrets` exception line from `.gitignore` (line 10). The `*.secrets` glob on line 9 will then correctly exclude all secrets files including rsyncd.secrets.

2. Run `git rm --cached config/rsyncd.secrets` to remove from git tracking while keeping the file on disk.

3. Create `config/rsyncd.secrets.example` with this exact content:
```
# LINBO rsync authentication
# Format: username:password
# Copy this file to rsyncd.secrets and change the password
linbo:CHANGE_ME
```

Verify the file is no longer tracked: `git ls-files config/rsyncd.secrets` should return empty.
Verify the file still exists on disk: `test -f config/rsyncd.secrets` should succeed.
  </action>
  <verify>
    <automated>cd /root/linbo-docker && git ls-files config/rsyncd.secrets | wc -l | grep -q '^0$' && test -f config/rsyncd.secrets && test -f config/rsyncd.secrets.example && grep -q 'CHANGE_ME' config/rsyncd.secrets.example && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>rsyncd.secrets is no longer tracked by git (git ls-files returns empty). The file still exists on disk for running containers. .gitignore no longer has the !config/rsyncd.secrets exception. config/rsyncd.secrets.example exists with linbo:CHANGE_ME placeholder.</done>
</task>

</tasks>

<verification>
1. Unit tests pass: `cd containers/api && npx jest tests/startup-validation.test.js --runInBand -x`
2. Full test suite still passes: `cd containers/api && npx jest --runInBand`
3. rsyncd.secrets not tracked: `git ls-files config/rsyncd.secrets` returns empty
4. rsyncd.secrets.example exists: `test -f config/rsyncd.secrets.example`
5. .gitignore has no rsyncd.secrets exception: `grep -c '!config/rsyncd.secrets' .gitignore` returns 0
</verification>

<success_criteria>
- validateSecrets() blocks API start in NODE_ENV=production with any known default JWT_SECRET or INTERNAL_API_KEY value
- validateSecrets() warns but allows start in non-production environments
- All 7+ unit tests pass
- rsyncd.secrets removed from git tracking, .example file created
- Existing test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/02-secrets-hardening/02-01-SUMMARY.md`
</output>
