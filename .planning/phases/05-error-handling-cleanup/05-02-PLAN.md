---
phase: 05-error-handling-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - containers/api/src/routes/configs.js
  - containers/api/src/routes/internal.js
  - containers/api/src/routes/images.js
  - containers/api/src/routes/auth.js
  - containers/api/src/routes/sync.js
  - containers/api/src/routes/patchclass.js
  - containers/api/src/routes/system/kernel.js
  - containers/api/src/routes/system/grub-theme.js
  - containers/api/src/middleware/auth.js
  - containers/api/src/middleware/audit.js
  - containers/api/src/index.js
autonomous: true
requirements:
  - DEBT-01

must_haves:
  truths:
    - "Every catch block in routes/, middleware/, and index.js either logs meaningfully or has a clarifying comment"
    - "No console.warn fires during normal startup or shutdown from routes/middleware/index.js"
    - "Prisma-optional require catches use console.debug with sync-mode message"
    - "Zero uncommented silent catches remain in the entire API codebase"
  artifacts:
    - path: "containers/api/src/routes/configs.js"
      provides: "4 categorized catch blocks (debug for file cleanup, warn for GRUB deletion)"
    - path: "containers/api/src/routes/internal.js"
      provides: "2 categorized catch blocks with once-flag pattern for Redis/Prisma fallback"
    - path: "containers/api/src/index.js"
      provides: "5 categorized catch blocks (debug for Prisma-optional, file guards, shutdown cleanup)"
    - path: "containers/api/src/routes/images.js"
      provides: "1 Prisma-optional debug catch"
    - path: "containers/api/src/routes/auth.js"
      provides: "1 Prisma-optional debug catch"
    - path: "containers/api/src/middleware/auth.js"
      provides: "1 Prisma-optional debug catch"
    - path: "containers/api/src/middleware/audit.js"
      provides: "1 Prisma-optional debug catch"
    - path: "containers/api/src/routes/sync.js"
      provides: "1 categorized catch block (debug for settings check)"
    - path: "containers/api/src/routes/patchclass.js"
      provides: "1 categorized catch block (debug for file cleanup)"
    - path: "containers/api/src/routes/system/kernel.js"
      provides: "1 categorized catch block (warn for background rebuild)"
    - path: "containers/api/src/routes/system/grub-theme.js"
      provides: "1 categorized catch block (debug for file cleanup)"
  key_links:
    - from: "containers/api/src/routes/internal.js"
      to: "once-flag pattern"
      via: "module-scoped let _redisWarnLogged = false"
      pattern: "_redisWarnLogged"
    - from: "containers/api/src/index.js"
      to: "Prisma-optional + startup catches"
      via: "console.debug for expected failures"
      pattern: "console\\.debug.*Prisma\\|console\\.debug.*Startup"
---

<objective>
Replace all 19 silent catch blocks in routes, middleware, and index.js with categorized logging.

Purpose: Complete the catch block cleanup across the remaining 11 files. This plan covers Prisma-optional require patterns (4 files), route-level catches (6 files), and the critical index.js startup/shutdown catches (5 blocks). Combined with Plan 01 (services), this eliminates all 48 silent catches in the codebase.

Output: 11 modified files with zero remaining silent catches across the entire API codebase.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-error-handling-cleanup/05-CONTEXT.md
@.planning/phases/05-error-handling-cleanup/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Categorize catches in Prisma-optional modules and route files</name>
  <files>containers/api/src/routes/images.js, containers/api/src/routes/auth.js, containers/api/src/middleware/auth.js, containers/api/src/middleware/audit.js, containers/api/src/routes/configs.js, containers/api/src/routes/sync.js, containers/api/src/routes/patchclass.js, containers/api/src/routes/system/kernel.js, containers/api/src/routes/system/grub-theme.js</files>
  <action>
Apply categorized logging to all 14 silent catch blocks across these 9 files:

**Prisma-optional requires (4 files, 1 catch each) -- console.debug with sync-mode message:**
- routes/images.js (~line 19): `try { prisma = require(...) } catch {}` -> `catch { console.debug('[Images] Prisma not available, running in sync mode'); }`
- routes/auth.js (~line 17): `try { prisma = require(...) } catch {}` -> `catch { console.debug('[Auth] Prisma not available, running in sync mode'); }`
- middleware/auth.js (~line 15): `try { prisma = require(...) } catch {}` -> `catch { console.debug('[Auth] Prisma not available, running in sync mode'); }`
- middleware/audit.js (~line 10): `try { prisma = require(...) } catch {}` -> `catch { console.debug('[Audit] Prisma not available, running in sync mode'); }`

NOTE: Prisma-optional catches do NOT need the `(err)` parameter since they log a static message, not err.message. Keep `catch {` without parameter.

**configs.js (4 catches):**
- Line 408: `fsPromises.unlink(startConfPath).catch(() => {})` -> `.catch(err => console.debug('[Configs] cleanup: unlink start.conf failed:', err.message))`
- Line 409: `fsPromises.unlink(md5Path).catch(() => {})` -> `.catch(err => console.debug('[Configs] cleanup: unlink md5 failed:', err.message))`
- Line 424: `fsPromises.unlink(ipLink).catch(() => {})` -> `.catch(err => console.debug('[Configs] cleanup: unlink IP link failed:', err.message))`
- Line 429: GRUB host config deletion `.catch(() => {})` -> `.catch(err => console.warn('[Configs] GRUB host config deletion failed:', err.message))`

**sync.js route (1 catch):**
- Line 42: Settings sync_enabled check `catch {}` -> `catch (err) { console.debug('[Sync] settings check failed:', err.message); }`

**patchclass.js (1 catch):**
- Line 37: `fs.unlink(filePath).catch(() => {})` -> `.catch(err => console.debug('[Patchclass] cleanup: unlink temp file failed:', err.message))`

**kernel.js (1 catch):**
- Line 129: Async IIFE kernel rebuild `.catch(() => {})` -> `.catch(err => console.warn('[Kernel] background rebuild failed:', err.message))`

**grub-theme.js (1 catch):**
- Line 28: `fs.unlink(filePath).catch(() => {})` -> `.catch(err => console.debug('[GrubTheme] cleanup: unlink temp file failed:', err.message))`

IMPORTANT: Line numbers are approximate. Find catches by searching for `catch {}` and `.catch(() => {})`.
  </action>
  <verify>
    <automated>cd /root/linbo-docker && grep -rn 'catch\s*{}\|\.catch(() => {})' containers/api/src/routes/ containers/api/src/middleware/auth.js containers/api/src/middleware/audit.js | grep -v '// WS broadcast' | wc -l</automated>
  </verify>
  <done>Zero uncommented silent catch blocks remain in routes/ and middleware/. Prisma-optional catches use console.debug with static sync-mode message. File cleanup uses console.debug. Service call failures use console.warn. Background ops use console.warn.</done>
</task>

<task type="auto">
  <name>Task 2: Categorize catches in index.js and run once-flag pattern in internal.js, then verify full codebase</name>
  <files>containers/api/src/index.js, containers/api/src/routes/internal.js</files>
  <action>
Apply categorized logging to all 7 silent catch blocks across these 2 files:

**index.js (5 catches):**
- Line 593: `try { settingsService.get('sync_enabled') } catch {}` -- This is a startup flow control catch (determines sync mode). Use: `catch (err) { console.debug('[Startup] settings service unavailable, determining sync mode:', err.message); }`
- Line 678: `sanityFs.unlinkSync(runningMarker) catch {}` -> `catch (err) { console.debug('[AutoRebuild] cleanup: unlink running marker failed:', err.message); }`
- Line 681: `sanityFs.renameSync(runningMarker, rebuildMarker) catch {}` -> `catch (err) { console.debug('[AutoRebuild] restore rebuild marker failed:', err.message); }`
- Line 685: `sanityFs.renameSync(runningMarker, rebuildMarker) catch {}` -> `catch (err) { console.debug('[AutoRebuild] restore rebuild marker on error failed:', err.message); }`
- Line 737: `termService.destroyAll() catch {}` -> `catch (err) { console.debug('[Shutdown] terminal cleanup failed:', err.message); }`

**internal.js (2 catches) -- once-flag pattern per user decision:**
Add at module scope (top of file, after imports): `let _redisWarnLogged = false;`

- Line 754: Redis host lookup in findHostByIp `catch {}` -> `catch (err) { if (!_redisWarnLogged) { console.debug('[Internal] Redis host lookup failed, using Prisma fallback:', err.message); _redisWarnLogged = true; } }`
- Line 771: Prisma host lookup fallback `catch {}` -> `catch (err) { console.debug('[Internal] Prisma host lookup also failed:', err.message); }`

The second catch (Prisma fallback) does NOT need once-flag since it only fires when both Redis AND Prisma fail, which is genuinely notable. Debug level is appropriate (the function returns null gracefully).

**After all changes, run the full verification:**
1. Grep the entire src/ directory for remaining silent catches
2. Run the full test suite
3. Verify grep count matches expected (only WS broadcast comments should remain)
  </action>
  <verify>
    <automated>cd /root/linbo-docker && SILENT=$(grep -rn 'catch\s*{}\|\.catch(() => {})' containers/api/src/ | grep -v '// WS broadcast' | grep -v '// Already logged' | wc -l) && echo "Remaining silent catches: $SILENT" && test "$SILENT" -eq 0 && echo "PASS" || echo "FAIL" && cd containers/api && npx jest --runInBand 2>&1 | tail -5</automated>
  </verify>
  <done>Zero uncommented silent catch blocks remain in the entire containers/api/src/ codebase. The once-flag pattern is applied to internal.js Redis lookup. All index.js startup/shutdown catches use console.debug. Full test suite passes. The phase gate (DEBT-01) is satisfied: every catch block either logs meaningfully, rethrows, or has a WS broadcast comment.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn 'catch\s*{}\|\.catch(() => {})' containers/api/src/ | grep -v '// WS broadcast' | grep -v '// Already logged' | wc -l` returns 0
2. `cd containers/api && npx jest --runInBand` passes
3. WS broadcast catches (7 total) all have `// WS broadcast: no clients is normal` comment
</verification>

<success_criteria>
- All 19 silent catch blocks in routes/, middleware/, and index.js are categorized
- Prisma-optional require catches use `console.debug('[Module] Prisma not available, running in sync mode')`
- internal.js uses once-flag pattern (`_redisWarnLogged`) for Redis fallback logging
- index.js startup/shutdown catches use `console.debug` (these are expected-failure paths)
- kernel.js background rebuild uses `console.warn` (fire-and-forget background op)
- configs.js GRUB deletion uses `console.warn` (service call failure)
- Full test suite passes without regression
- Combined with Plan 01: zero silent catches remain across entire API codebase (DEBT-01 complete)
</success_criteria>

<output>
After completion, create `.planning/phases/05-error-handling-cleanup/05-02-SUMMARY.md`
</output>
