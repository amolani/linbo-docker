---
phase: 05-error-handling-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - containers/api/src/services/linbo-update.service.js
  - containers/api/src/services/sync.service.js
  - containers/api/src/services/image-sync.service.js
  - containers/api/src/services/terminal.service.js
  - containers/api/src/services/settings.service.js
  - containers/api/src/services/deviceImport.service.js
  - containers/api/src/services/remote.service.js
  - containers/api/src/services/sync-operations.service.js
autonomous: true
requirements:
  - DEBT-01

must_haves:
  truths:
    - "Every catch block in services/ either logs meaningfully or has a WS broadcast comment"
    - "No console.warn fires during normal sync-mode or standalone-mode idle operation from service files"
    - "Existing test suite passes without new warnings"
  artifacts:
    - path: "containers/api/src/services/linbo-update.service.js"
      provides: "11 categorized catch blocks (debug for file cleanup/data-fetch, warn for background ops)"
    - path: "containers/api/src/services/sync.service.js"
      provides: "6 categorized catch blocks (WS broadcast comments, debug for health check)"
    - path: "containers/api/src/services/image-sync.service.js"
      provides: "3 categorized catch blocks (debug for file cleanup/progress)"
    - path: "containers/api/src/services/terminal.service.js"
      provides: "3 categorized catch blocks (debug for stream/connection cleanup)"
    - path: "containers/api/src/services/settings.service.js"
      provides: "2 WS broadcast comment-only catches"
    - path: "containers/api/src/services/deviceImport.service.js"
      provides: "2 categorized catch blocks (warn for GRUB config generation)"
    - path: "containers/api/src/services/remote.service.js"
      provides: "1 categorized catch block (warn for SSH gui_ctl restore)"
    - path: "containers/api/src/services/sync-operations.service.js"
      provides: "1 categorized catch block (debug for mkdir prerequisite)"
  key_links:
    - from: "containers/api/src/services/sync.service.js"
      to: "WS broadcast catches"
      via: "comment-only pattern: // WS broadcast: no clients is normal"
      pattern: "WS broadcast"
    - from: "containers/api/src/services/linbo-update.service.js"
      to: "console.debug/warn calls"
      via: "categorized logging per decision tree"
      pattern: "console\\.(debug|warn)"
---

<objective>
Replace all 29 silent catch blocks in API service files with categorized logging.

Purpose: Observability improvement for the 8 service files that contain silent catches. Services are the core business logic and have the highest concentration of silent catches (29 of 48 total). Fixing services first establishes the pattern for the route/middleware cleanup in Plan 02.

Output: 8 modified service files with zero remaining silent catches (except WS broadcast comment-only exceptions).
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-error-handling-cleanup/05-CONTEXT.md
@.planning/phases/05-error-handling-cleanup/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Categorize catches in heavy service files (linbo-update, sync, image-sync)</name>
  <files>containers/api/src/services/linbo-update.service.js, containers/api/src/services/sync.service.js, containers/api/src/services/image-sync.service.js</files>
  <action>
Apply categorized logging to all 20 silent catch blocks across these 3 files. Use the exact categorization from 05-RESEARCH.md inventory:

**linbo-update.service.js (11 catches):**
- Line 72: Redis heartbeat expire `.catch(() => {})` -> `.catch(err => console.debug('[LinboUpdate] heartbeat expire failed:', err.message))`
- Line 86: Redis lock release `.catch(() => {})` -> `.catch(err => console.debug('[LinboUpdate] lock release failed:', err.message))`
- Line 205: Gzip decompression `catch {}` -> `catch (err) { console.debug('[LinboUpdate] gzip decompression failed, using plain body:', err.message); }`
- Line 250: Read installed version `catch {}` -> `catch (err) { console.debug('[LinboUpdate] read installed version failed:', err.message); }`
- Line 362: WS progress broadcast `.catch(() => {})` -> `.catch(() => {}) // WS broadcast: no clients is normal` (comment-only per user decision)
- Line 375: Unlink debPath after SHA mismatch `.catch(() => {})` -> `.catch(err => console.debug('[LinboUpdate] cleanup: unlink deb failed:', err.message))`
- Line 379: Unlink debPath after size mismatch `.catch(() => {})` -> `.catch(err => console.debug('[LinboUpdate] cleanup: unlink deb failed:', err.message))`
- Line 480: Unlink symlink before recreate `catch {}` -> `catch (err) { console.debug('[LinboUpdate] cleanup: unlink symlink failed:', err.message); }`
- Line 488: Unlink icons symlink `catch {}` -> `catch (err) { console.debug('[LinboUpdate] cleanup: unlink icons symlink failed:', err.message); }`
- Line 602: Unlink/rm currentLink `catch {}` -> `catch (err) { console.debug('[LinboUpdate] cleanup: unlink current link failed:', err.message); }`
- Line 613: Old set cleanup readdir `catch {}` -> `catch (err) { console.debug('[LinboUpdate] cleanup: readdir old set failed:', err.message); }`

**sync.service.js (6 catches):**
- Line 59: WS broadcast sync.started `catch {}` -> `catch {} // WS broadcast: no clients is normal`
- Line 101: WS broadcast sync.progress `catch {}` -> `catch {} // WS broadcast: no clients is normal`
- Line 168: WS broadcast sync.progress `catch {}` -> `catch {} // WS broadcast: no clients is normal`
- Line 259: WS broadcast sync.completed `catch {}` -> `catch {} // WS broadcast: no clients is normal`
- Line 266: WS broadcast sync.failed `catch {}` -> `catch {} // WS broadcast: no clients is normal`
- Line 481: LMN API health check `catch {}` -> `catch (err) { console.debug('[Sync] LMN API health check failed:', err.message); }`

**image-sync.service.js (3 catches):**
- Line 451: Unlink partPath `.catch(() => {})` -> `.catch(err => console.debug('[ImageSync] cleanup: unlink partial file failed:', err.message))`
- Line 496: WS broadcast download progress `.catch(() => {})` -> `.catch(() => {}) // WS broadcast: no clients is normal`
- Line 503: Redis set download progress `.catch(() => {})` -> `.catch(err => console.debug('[ImageSync] Redis progress update failed:', err.message))`

IMPORTANT: Line numbers are approximate -- find the actual catch blocks by searching for `catch {}` and `.catch(() => {})` in each file. The context around each catch (variable names, surrounding code) identifies which one.

Do NOT refactor inline `.catch()` to try/catch blocks. Keep the pattern as-is per user decision.
  </action>
  <verify>
    <automated>cd /root/linbo-docker && grep -rn 'catch\s*{}' containers/api/src/services/linbo-update.service.js containers/api/src/services/sync.service.js containers/api/src/services/image-sync.service.js | grep -v '// WS broadcast' | wc -l</automated>
  </verify>
  <done>Zero uncommented silent catch blocks remain in linbo-update.service.js, sync.service.js, and image-sync.service.js. All WS broadcast catches have the comment. All file cleanup catches use console.debug. All data-fetch fallbacks use console.debug.</done>
</task>

<task type="auto">
  <name>Task 2: Categorize catches in remaining service files (terminal, settings, deviceImport, remote, sync-operations)</name>
  <files>containers/api/src/services/terminal.service.js, containers/api/src/services/settings.service.js, containers/api/src/services/deviceImport.service.js, containers/api/src/services/remote.service.js, containers/api/src/services/sync-operations.service.js</files>
  <action>
Apply categorized logging to all 9 silent catch blocks across these 5 files:

**terminal.service.js (3 catches):**
- Line 29: Fallback SSH key read `catch {}` -> `catch {} // Already logged warn on line above` (this catch is after a console.warn, so just add a clarifying comment -- the error is already reported)
- Line 197: `session.stream.end()` during destroy `catch {}` -> `catch (err) { console.debug('[Terminal] cleanup: stream end failed:', err.message); }`
- Line 198: `session.client.end()` during destroy `catch {}` -> `catch (err) { console.debug('[Terminal] cleanup: client end failed:', err.message); }`

**settings.service.js (2 catches):**
- Line 138: WS broadcast settings.changed `catch {}` -> `catch {} // WS broadcast: no clients is normal`
- Line 158: WS broadcast settings.changed `catch {}` -> `catch {} // WS broadcast: no clients is normal`

**deviceImport.service.js (2 catches):**
- Line 567: GRUB config generation `.catch(() => {})` -> `.catch(err => console.warn('[DeviceImport] GRUB config generation failed:', err.message))`
- Line 573: Host GRUB config generation `.catch(() => {})` -> `.catch(err => console.warn('[DeviceImport] host GRUB config generation failed:', err.message))`

**remote.service.js (1 catch):**
- Line 548: SSH gui_ctl restore `.catch(() => {})` -> `.catch(err => console.warn('[Remote] gui_ctl restore failed:', err.message))`

**sync-operations.service.js (1 catch):**
- Line 537: mkdir LINBOCMD_DIR `.catch(() => {})` -> `.catch(err => console.debug('[SyncOps] mkdir LINBOCMD_DIR failed:', err.message))`

IMPORTANT: Line numbers are approximate -- find the actual catch blocks by searching for `catch {}` and `.catch(() => {})` in each file.

After completing changes, run the full test suite to verify no regressions.
  </action>
  <verify>
    <automated>cd /root/linbo-docker && grep -rn 'catch\s*{}\|\.catch(() => {})' containers/api/src/services/terminal.service.js containers/api/src/services/settings.service.js containers/api/src/services/deviceImport.service.js containers/api/src/services/remote.service.js containers/api/src/services/sync-operations.service.js | grep -v '// WS broadcast' | grep -v '// Already logged' | wc -l</automated>
  </verify>
  <done>Zero uncommented silent catch blocks remain in terminal.service.js, settings.service.js, deviceImport.service.js, remote.service.js, and sync-operations.service.js. WS broadcast catches have comments. Service call failures use console.warn. Stream/connection cleanup uses console.debug.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn 'catch\s*{}\|\.catch(() => {})' containers/api/src/services/ | grep -v '// WS broadcast' | grep -v '// Already logged' | wc -l` returns 0
2. `cd containers/api && npx jest --runInBand` passes with no new warnings
</verification>

<success_criteria>
- All 29 silent catch blocks in services/ are categorized with logging or comments
- WS broadcast catches retain `.catch(() => {})` with `// WS broadcast: no clients is normal` comment
- File cleanup catches use `console.debug('[ServiceName] cleanup: ...')` format
- Service call failures and background ops use `console.warn('[ServiceName] ...')` format
- Data-fetch fallbacks use `console.debug`
- Test suite passes without regression
</success_criteria>

<output>
After completion, create `.planning/phases/05-error-handling-cleanup/05-01-SUMMARY.md`
</output>
