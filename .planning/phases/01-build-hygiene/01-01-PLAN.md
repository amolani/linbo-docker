---
phase: 01-build-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - containers/api/Dockerfile
  - containers/web/Dockerfile
  - containers/init/Dockerfile
  - containers/tftp/Dockerfile
  - containers/rsync/Dockerfile
  - containers/ssh/Dockerfile
  - containers/dhcp/Dockerfile
  - docker-compose.yml
  - containers/api/.dockerignore
  - containers/web/.dockerignore
  - containers/init/.dockerignore
  - containers/tftp/.dockerignore
  - containers/rsync/.dockerignore
  - containers/ssh/.dockerignore
  - containers/dhcp/.dockerignore
autonomous: true
requirements:
  - PROD-01
  - PROD-03

must_haves:
  truths:
    - "Every FROM line in every Dockerfile uses an exact version tag (no floating tags like :latest, :alpine, :20-alpine)"
    - "docker-compose.yml redis image uses an exact version tag"
    - "Every container directory with a Dockerfile also has a .dockerignore file"
    - "Building the api container from a directory containing node_modules/ does not copy those host artifacts into the image"
  artifacts:
    - path: "containers/api/Dockerfile"
      provides: "Pinned base images for api builder and production stages"
      contains: "node:20.20.0-alpine"
    - path: "containers/web/Dockerfile"
      provides: "Pinned base images for web builder and nginx stages"
      contains: "nginx:1.29.5-alpine"
    - path: "containers/init/Dockerfile"
      provides: "Pinned base image for init container"
      contains: "alpine:3.19.9"
    - path: "containers/tftp/Dockerfile"
      provides: "Pinned base image for tftp container"
      contains: "ubuntu:24.04"
    - path: "containers/rsync/Dockerfile"
      provides: "Pinned base image for rsync container"
      contains: "ubuntu:24.04"
    - path: "containers/ssh/Dockerfile"
      provides: "Pinned base image for ssh container"
      contains: "ubuntu:24.04"
    - path: "containers/dhcp/Dockerfile"
      provides: "Pinned base image for dhcp container"
      contains: "alpine:3.19.9"
    - path: "docker-compose.yml"
      provides: "Pinned redis image"
      contains: "redis:7.4.7-alpine"
    - path: "containers/api/.dockerignore"
      provides: "Build context exclusions for api"
    - path: "containers/web/.dockerignore"
      provides: "Build context exclusions for web"
    - path: "containers/init/.dockerignore"
      provides: "Build context exclusions for init"
    - path: "containers/tftp/.dockerignore"
      provides: "Build context exclusions for tftp"
    - path: "containers/rsync/.dockerignore"
      provides: "Build context exclusions for rsync"
    - path: "containers/ssh/.dockerignore"
      provides: "Build context exclusions for ssh"
    - path: "containers/dhcp/.dockerignore"
      provides: "Build context exclusions for dhcp"
  key_links:
    - from: "docker-compose.yml"
      to: "containers/*/Dockerfile"
      via: "build context references"
      pattern: "build:.*context:"
---

<objective>
Pin all Docker base images to exact version tags and add .dockerignore files to every container directory.

Purpose: Ensure reproducible builds and prevent host artifacts (node_modules, .git, .env) from contaminating Docker build contexts.
Output: 7 updated Dockerfiles, 1 updated docker-compose.yml, 7 new .dockerignore files.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-hygiene/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin all Docker base images to exact version tags</name>
  <files>
    containers/api/Dockerfile,
    containers/web/Dockerfile,
    containers/init/Dockerfile,
    containers/tftp/Dockerfile,
    containers/rsync/Dockerfile,
    containers/ssh/Dockerfile,
    containers/dhcp/Dockerfile,
    docker-compose.yml
  </files>
  <action>
Update every FROM line in every Dockerfile and the redis image in docker-compose.yml to use exact version-pinned tags. No SHA256 digests (per user decision). Use these exact versions (verified against locally pulled images):

**Alpine-based containers:**
- `containers/api/Dockerfile`: Both FROM lines (`node:20-alpine AS builder` and `FROM node:20-alpine`) become `node:20.20.0-alpine3.21`
- `containers/web/Dockerfile`: Builder FROM `node:20-alpine` becomes `node:20.20.0-alpine3.21`. Serve FROM `nginx:alpine` becomes `nginx:1.29.5-alpine`
- `containers/init/Dockerfile`: FROM `alpine:3.19` becomes `alpine:3.19.9`
- `containers/dhcp/Dockerfile`: FROM `alpine:3.19` becomes `alpine:3.19.9`

**Ubuntu-based containers:**
- `containers/tftp/Dockerfile`: FROM `ubuntu:24.04` stays `ubuntu:24.04` (this IS the most specific Docker Hub tag available for Ubuntu; no sub-patch tags exist)
- `containers/rsync/Dockerfile`: Same -- `ubuntu:24.04` stays
- `containers/ssh/Dockerfile`: Same -- `ubuntu:24.04` stays

**docker-compose.yml:**
- `image: redis:7-alpine` becomes `image: redis:7.4.7-alpine`

Add a comment above each FROM line with the pin date for future updaters:
```dockerfile
# Pinned 2026-03-06 — update periodically
FROM node:20.20.0-alpine3.21 AS builder
```

Do NOT change anything else in the Dockerfiles -- no package changes, no layer changes, no ordering changes. Only FROM lines and the redis image tag.
  </action>
  <verify>
    <automated>cd /root/linbo-docker && grep -rn '^FROM ' containers/*/Dockerfile | grep -vE ':[0-9]+\.[0-9]+' | grep -v 'ubuntu:24.04' && echo "FAIL: unpinned FROM found" || echo "PASS: all FROM lines pinned"; grep 'image: redis:' docker-compose.yml | grep -v '7.4.7' && echo "FAIL: redis unpinned" || echo "PASS: redis pinned"</automated>
  </verify>
  <done>Every FROM line in all 7 Dockerfiles uses an exact version tag (no floating :latest, :alpine, :20-alpine). docker-compose.yml redis image is pinned to 7.4.7-alpine. Ubuntu images stay at 24.04 (most specific tag available).</done>
</task>

<task type="auto">
  <name>Task 2: Add .dockerignore files to all container directories</name>
  <files>
    containers/api/.dockerignore,
    containers/web/.dockerignore,
    containers/init/.dockerignore,
    containers/tftp/.dockerignore,
    containers/rsync/.dockerignore,
    containers/ssh/.dockerignore,
    containers/dhcp/.dockerignore
  </files>
  <action>
Create a .dockerignore file in each of the 7 container directories. Goal is preventing host contamination, not build optimization (per user decision). Keep it minimal with standard exclusions.

**For containers/api/.dockerignore** (Node.js, has node_modules and tests locally):
```
node_modules
.git
.env
.env.*
*.md
tests
Dockerfile
.dockerignore
jest.config.js
coverage
.nyc_output
```

**For containers/web/.dockerignore** (Node.js builder + nginx, has frontend/node_modules):
```
node_modules
frontend/node_modules
.git
.env
.env.*
*.md
Dockerfile
.dockerignore
```
Note: The web Dockerfile COPYs `frontend/` dir, so `node_modules` at both root and frontend level must be excluded.

**For containers/init/.dockerignore** (simple Alpine + shell script):
```
.git
.env
*.md
Dockerfile
.dockerignore
```

**For containers/tftp/.dockerignore** (Ubuntu + shell script + config):
```
.git
.env
*.md
Dockerfile
.dockerignore
```

**For containers/rsync/.dockerignore** (Ubuntu + scripts dir):
```
.git
.env
*.md
Dockerfile
.dockerignore
```

**For containers/ssh/.dockerignore** (Ubuntu + shell scripts):
```
.git
.env
*.md
Dockerfile
.dockerignore
```

**For containers/dhcp/.dockerignore** (Alpine + shell scripts):
```
.git
.env
*.md
Dockerfile
.dockerignore
```

Do NOT add any other exclusions beyond what is listed. The goal is minimal host contamination prevention.
  </action>
  <verify>
    <automated>cd /root/linbo-docker && MISSING=""; for dir in containers/api containers/web containers/init containers/tftp containers/rsync containers/ssh containers/dhcp; do [ -f "$dir/.dockerignore" ] || MISSING="$MISSING $dir"; done; [ -z "$MISSING" ] && echo "PASS: all 7 .dockerignore files exist" || echo "FAIL: missing .dockerignore in:$MISSING"; grep -l 'node_modules' containers/api/.dockerignore containers/web/.dockerignore 2>/dev/null | wc -l | grep -q 2 && echo "PASS: node_modules excluded in api+web" || echo "FAIL: node_modules not excluded"</automated>
  </verify>
  <done>All 7 container directories have a .dockerignore file. The api and web containers exclude node_modules. All containers exclude .git, .env, *.md, Dockerfile, and .dockerignore.</done>
</task>

</tasks>

<verification>
Run the following checks after both tasks complete:

1. **No floating tags remain:**
   ```bash
   grep -rn '^FROM ' containers/*/Dockerfile | grep -E ':(latest|alpine)(\s|$)' | grep -v '#'
   ```
   Expected: no output (zero matches)

2. **All .dockerignore files exist:**
   ```bash
   for dir in containers/*/; do [ -f "$dir/.dockerignore" ] && echo "OK: $dir" || echo "MISSING: $dir"; done
   ```
   Expected: 7 "OK" lines, 0 "MISSING" lines

3. **Docker build still works (api as representative):**
   ```bash
   docker build --no-cache -t linbo-api-test containers/api/ 2>&1 | tail -5
   ```
   Expected: successful build (exit code 0)

4. **Host node_modules excluded from build context:**
   ```bash
   docker build --no-cache containers/api/ 2>&1 | grep -i 'sending build context' | head -1
   ```
   Expected: build context size significantly smaller than before (node_modules excluded)
</verification>

<success_criteria>
- Every Dockerfile FROM line uses an exact version-pinned tag (no :latest, :alpine, :20-alpine floating tags)
- docker-compose.yml redis image pinned to redis:7.4.7-alpine
- All 7 container directories contain a .dockerignore file
- api and web .dockerignore files exclude node_modules
- Docker builds still succeed after changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-hygiene/01-01-SUMMARY.md`
</output>
