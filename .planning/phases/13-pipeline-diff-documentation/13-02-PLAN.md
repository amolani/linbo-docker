---
phase: 13-pipeline-diff-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/server/update-linbofs.sh
  - docs/UNTERSCHIEDE-ZU-LINBO.md
autonomous: true
requirements: [DIFF-04, DIFF-05]

must_haves:
  truths:
    - "docs/UNTERSCHIEDE-ZU-LINBO.md contains a 3-column table (LMN behavior / Docker behavior / Justification) covering all 16 structural divergences"
    - "update-linbofs.sh header comments document the concatenated CPIO+XZ format with extraction commands"
  artifacts:
    - path: "docs/UNTERSCHIEDE-ZU-LINBO.md"
      provides: "Divergence catalog with 3-column reference table"
      contains: "LMN Original.*Docker.*Begr"
    - path: "scripts/server/update-linbofs.sh"
      provides: "CPIO+XZ format documentation in header comments"
      contains: "LINBOFS64 ARCHIVE FORMAT"
  key_links:
    - from: "docs/UNTERSCHIEDE-ZU-LINBO.md"
      to: "scripts/server/update-linbofs.sh"
      via: "Divergence table references Docker build script behavior"
      pattern: "update-linbofs"
    - from: "scripts/server/update-linbofs.sh"
      to: "linbofs64 output"
      via: "Header documents the archive format produced by the script"
      pattern: "LINBOFS64 ARCHIVE FORMAT"
---

<objective>
Document the CPIO+XZ concatenation format in update-linbofs.sh header comments and add a structured 3-column divergence table to UNTERSCHIEDE-ZU-LINBO.md covering all 16 structural differences between Docker and LMN builds.

Purpose: Ensure future maintainers understand the archive format (preventing accidental breakage of the two-segment structure) and can quickly reference all intentional divergences in a structured table.

Output: Updated update-linbofs.sh header, updated UNTERSCHIEDE-ZU-LINBO.md with new section.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pipeline-diff-documentation/13-RESEARCH.md

Source files:
@scripts/server/update-linbofs.sh
@docs/UNTERSCHIEDE-ZU-LINBO.md

<interfaces>
<!-- update-linbofs.sh current header (lines 1-9):
#!/bin/bash
#
# LINBO Docker - Update-Linbofs Script
# Injects SSH-Keys, RSYNC-Password hash, and selected kernel modules into linbofs64
#
# Based on the original linuxmuster.net update-linbofs script
# Adapted for LINBO Docker standalone solution with kernel variant support
#

The new CPIO format documentation block should be inserted AFTER this header block,
BEFORE the `set -e` line (currently line 10).
-->

<!-- UNTERSCHIEDE-ZU-LINBO.md structure:
1. Uebersicht (feature comparison table)
   - Was ist LINBO?
2. Docker-exklusive Features (sections 2.1-2.7)
3. Infrastruktur-Verbesserungen (3.1, 3.2)
4. Boot-Kompatibilitaet
5. Modifizierte Vanilla-Dateien
6. Zusammenfassung

The new 3-column divergence table should be added as a NEW section between
the existing narrative and the Zusammenfassung, covering build-pipeline
structural differences (not Docker features, which are already in section 2).
-->

<!-- 16 divergences from research (MUST all appear in the table):
1. Dependency management (self-contained vs helperfunctions.sh)
2. Lock mechanism (flock vs file touch)
3. Firmware provisioning (config-file vs client log parsing)
4. Locale injection (not injected vs copy_locale())
5. CPIO format (two XZ segments vs single)
6. cpio ownership (--owner 0:0 vs root execution)
7. GUI themes (theme injection vs package themes only)
8. Custom linbo_gui (optional binary override vs none)
9. Build status marker (.linbofs-patch-status vs none)
10. Docker volume sync (copy to mountpoint vs N/A)
11. efipxe devicenames (not copied vs copied)
12. Custom inittab (not supported vs appending)
13. ISO creation (none vs make-linbo-iso.sh)
14. Backup before rebuild (linbofs64.bak vs none)
15. Size verification (10MB minimum vs none)
16. Hook execution (sorted, exported vars, warn-only vs unsorted, no exports, halt)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document CPIO+XZ format in update-linbofs.sh header</name>
  <files>scripts/server/update-linbofs.sh</files>
  <action>
Insert a CPIO format documentation block into `scripts/server/update-linbofs.sh` AFTER the existing header comment block (line 8, after "Adapted for LINBO Docker standalone solution with kernel variant support") and BEFORE the blank line + `set -e` (currently line 10). The new block should be:

```
#
# LINBOFS64 ARCHIVE FORMAT
# ========================
# The output linbofs64 file consists of TWO concatenated XZ-compressed CPIO
# segments. This is a standard Linux initramfs technique -- the kernel's
# initramfs loader (init/initramfs.c) processes concatenated compressed
# cpio archives sequentially.
#
# Segment 1: Main filesystem (all files from WORKDIR)
#   Created by: find . | cpio -H newc --owner 0:0 | xz --check=crc32 -T0
#   Contains: SSH keys, password hash, kernel modules, firmware, themes, hooks
#
# Segment 2: Device nodes (dev/console c5,1 + dev/null c1,3)
#   Created by: xz --check=crc32 < DEVNODES_CPIO (pre-built binary cpio fragment)
#   Reason: The build runs as non-root (uid 1001). cpio(1) cannot create
#   character device nodes without CAP_MKNOD. A pre-built cpio fragment
#   containing these nodes is appended as a separate compressed segment.
#
# To inspect the full archive:
#   xzcat linbofs64 | cpio -t          # lists ALL files (both segments)
#   xzcat linbofs64 | cpio -i -d       # extracts ALL files (both segments)
#
# Note: xzcat/xz -dc decompresses ALL concatenated XZ streams, not just the
# first. The cpio TRAILER!!! marker separates the two archives, and cpio
# processes them sequentially, merging the results.
#
# See also: docs/UNTERSCHIEDE-ZU-LINBO.md (divergence #5: CPIO format)
#
```

This adds the block between the existing header and the `set -e` line. Do NOT modify any other part of the script. This is documentation only, not a behavioral change.

IMPORTANT: Per CLAUDE.md rule "update-linbofs.sh selbst nur aendern bei Bugs im Build-Prozess, nicht fuer Inhalte" -- this change is a header comment (documentation), not a content change to linbofs64. It is acceptable because it does not modify any build behavior.
  </action>
  <verify>
    <automated>grep -q "LINBOFS64 ARCHIVE FORMAT" scripts/server/update-linbofs.sh && grep -q "Segment 1:" scripts/server/update-linbofs.sh && grep -q "Segment 2:" scripts/server/update-linbofs.sh && grep -q "xzcat linbofs64 | cpio -t" scripts/server/update-linbofs.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>update-linbofs.sh header contains CPIO+XZ format documentation with both segment descriptions, inspection commands, and explanation of why two segments are needed.</done>
</task>

<task type="auto">
  <name>Task 2: Add 3-column divergence table to UNTERSCHIEDE-ZU-LINBO.md</name>
  <files>docs/UNTERSCHIEDE-ZU-LINBO.md</files>
  <action>
Add a new section to `docs/UNTERSCHIEDE-ZU-LINBO.md` titled "Build-Pipeline: Strukturelle Unterschiede (update-linbofs.sh)" BEFORE the final "Zusammenfassung" section. This section contains the 3-column table required by DIFF-04.

Structure:

```markdown
## Build-Pipeline: Strukturelle Unterschiede (update-linbofs.sh)

Die folgende Tabelle zeigt alle strukturellen Unterschiede zwischen dem originalen LMN `update-linbofs` Script (Paket `linuxmuster-cachingserver-linbo7` v4.3.31) und der Docker-Variante (`scripts/server/update-linbofs.sh`). Das LMN-Original ist als Referenz gepinnt unter `scripts/server/update-linbofs-lmn-original.sh`.

> **Hinweis:** Diese Tabelle betrifft nur den Build-Prozess (update-linbofs.sh), nicht die Docker-exklusiven Features (Patchclass, Firmware-Scan, Web-UI etc.), die in den obigen Abschnitten beschrieben sind.

| # | Bereich | LMN Original | Docker | Begruendung |
|---|---------|-------------|--------|-------------|
| 1 | Abhaengigkeiten | `source helperfunctions.sh` (braucht vollen LMN-Stack) | Eigenstaendiger Config-Block | Docker hat kein linuxmuster-base7-Paket |
| 2 | Lock-Mechanismus | Datei-basiert (`/tmp/.update-linbofs.lock`, touch+rm) | flock-basiert (fd 8, `CONFIG_DIR/.rebuild.lock`) | Race-Condition-sicher fuer shared Docker Volumes |
| 3 | Firmware-Provisioning | Download von kernel.org + Parsen von LINBO-Client-Logs | Config-Datei-basiert mit Pfad-Traversal-Schutz, zst-Dekompression, Symlink-Checks | Kein Client-Log-Zugriff in Docker; Security-gehaertet |
| 4 | Locale-Injection | `copy_locale()` -- volle Locale-Unterstuetzung mit chroot locale-gen | Nicht injiziert | Docker-Clients brauchen keine Locale im linbofs |
| 5 | CPIO-Format | Ein XZ-Segment: `find . \| cpio \| xz` | Zwei XZ-Segmente: Hauptinhalt + Device-Nodes | Non-Root-Build (uid 1001) kann kein mknod |
| 6 | CPIO-Ownership | Laeuft als root, kein --owner-Flag noetig | `--owner 0:0` Flag | Garantiert Root-Ownership trotz Non-Root-Build |
| 7 | GUI-Themes | Paket-Themes, kein Injektionsmechanismus | Theme-Injection aus `$LINBO_DIR/gui-themes/` | Docker unterstuetzt Custom-Branding |
| 8 | Custom linbo_gui | Kein Custom-Binary-Support | Optionaler Binary-Override aus `$CONFIG_DIR/linbo_gui` | Docker unterstuetzt Custom-GUI-Builds |
| 9 | Build-Status-Marker | Kein Marker | `.linbofs-patch-status` nach erfolgreichem Build | TFTP-Container wartet auf diesen Marker |
| 10 | Docker-Volume-Sync | Nicht anwendbar | Kopiert zu Docker-Volume-Mountpoint falls abweichend von LINBO_DIR | Stellt sicher, dass TFTP aktualisierte Dateien ausliefert |
| 11 | efipxe devicenames | Kopiert `efipxe` nach `usr/share/linbo` | Nicht kopiert | Docker nutzt GRUB HTTP Boot, efipxe nicht noetig |
| 12 | Custom inittab | Unterstuetzt Anfuegen von `$LINBOSYSDIR/inittab` | Nicht unterstuetzt | Docker-linbofs nutzt Standard-inittab |
| 13 | ISO-Erstellung | Ruft `make-linbo-iso.sh` nach Build auf | Keine ISO-Erstellung | Docker liefert via TFTP/HTTP, kein ISO noetig |
| 14 | Backup vor Rebuild | Kein Backup | Erstellt `linbofs64.bak` vor Rebuild | Rollback-Faehigkeit fuer Docker-Deployments |
| 15 | Groessenverifikation | Keine Groessenpruefung | Minimum 10MB-Check auf neue Datei | Verhindert Deploy von korruptem/leerem linbofs64 |
| 16 | Hook-Ausfuehrung | Unsortiertes find, keine exportierten Vars, Fehler koennen Build stoppen | Sortierte Ausfuehrung, exportierte Vars, Fehler warnen aber stoppen nicht | Verbesserte Zuverlaessigkeit und Hook-Developer-Experience |
```

Preserve ALL existing content in the file. Insert this new section BEFORE the final "Zusammenfassung" section. Do not modify or remove any existing sections.

Also fix the duplicate section numbering issue in the table of contents (there are currently two "5." entries and the "Zusammenfassung" numbering is inconsistent). Renumber to:
1. Uebersicht
2. Docker-exklusive Features
3. Infrastruktur-Verbesserungen
4. Boot-Kompatibilitaet
5. Modifizierte Vanilla-Dateien
6. Build-Pipeline: Strukturelle Unterschiede (NEW)
7. Zusammenfassung

Update both the ToC and the section headers to match.
  </action>
  <verify>
    <automated>grep -q "Strukturelle Unterschiede" docs/UNTERSCHIEDE-ZU-LINBO.md && grep -c "| [0-9]" docs/UNTERSCHIEDE-ZU-LINBO.md | xargs test 16 -le && grep -q "LMN Original.*Docker.*Begr" docs/UNTERSCHIEDE-ZU-LINBO.md && grep -q "flock-basiert" docs/UNTERSCHIEDE-ZU-LINBO.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>UNTERSCHIEDE-ZU-LINBO.md contains a 3-column table with all 16 structural divergences between LMN and Docker build pipelines, properly numbered and integrated into the existing document structure.</done>
</task>

</tasks>

<verification>
1. `grep -q "LINBOFS64 ARCHIVE FORMAT" scripts/server/update-linbofs.sh` -- CPIO format documented
2. `grep -q "Segment 1:" scripts/server/update-linbofs.sh` -- Both segments described
3. `grep -q "Strukturelle Unterschiede" docs/UNTERSCHIEDE-ZU-LINBO.md` -- New section exists
4. `grep -c "^| [0-9]" docs/UNTERSCHIEDE-ZU-LINBO.md` -- At least 16 table rows
5. `grep -q "LMN Original.*Docker.*Begr" docs/UNTERSCHIEDE-ZU-LINBO.md` -- 3-column headers present
6. `bash -n scripts/server/update-linbofs.sh` -- Script still valid bash (header comments didn't break syntax)
</verification>

<success_criteria>
- update-linbofs.sh header contains complete CPIO+XZ format documentation with extraction commands
- UNTERSCHIEDE-ZU-LINBO.md contains a 3-column table covering all 16 structural divergences
- Existing narrative content in UNTERSCHIEDE-ZU-LINBO.md is preserved
- Section numbering is consistent throughout the document
</success_criteria>

<output>
After completion, create `.planning/phases/13-pipeline-diff-documentation/13-02-SUMMARY.md`
</output>
